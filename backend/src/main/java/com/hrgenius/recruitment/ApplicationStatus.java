package com.hrgenius.recruitment;

/** Application pipeline stage (matches CK_APP_STATUS). */
public enum ApplicationStatus {
    APPLIED,
    SCREENING,
    SHORTLISTED,
    INTERVIEW,
    SELECTED,
    REJECTED
}
