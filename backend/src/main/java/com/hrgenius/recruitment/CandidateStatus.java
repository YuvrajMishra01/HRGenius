package com.hrgenius.recruitment;

/** Candidate pipeline status (matches CK_CAND_STATUS). */
public enum CandidateStatus {
    NEW,
    SCREENING,
    SHORTLISTED,
    INTERVIEWED,
    SELECTED,
    REJECTED,
    HIRED
}
